query = """
SELECT
    COUNT(m.movie_id) AS total_movies,
    AVG(m.renting_price) AS average_renting_price,
    AVG(r.rating) AS average_rating
FROM movies m
LEFT JOIN rentings r
    ON m.movie_id = r.movie_id;
"""

run_query(query)
